name: TEMP RAVEMEMS REPORT TEST2 NAMEERROR

on:
  push:
    branches:
      - RAPPORT
    paths:
      - .ravemems-report-test2-nameerror-trigger

permissions:
  contents: write

jobs:
  append:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: RAPPORT
          fetch-depth: 0
      - run: python tools/temp_append_ravemems_test2_nameerror.py
      - run: |
          set -euo pipefail
          git config user.name github-actions-bot
          git config user.email github-actions@users.noreply.github.com
          git add RAPPORT_SUIVI_ECU_MEMS_MANAGER.md
          git rm .github/workflows/temp-ravemems-report-test2-nameerror.yml tools/temp_append_ravemems_test2_nameerror.py .ravemems-report-test2-nameerror-trigger
          git commit -m "Record TEST2 completeness guard NameError"
          git push origin RAPPORT
