name: TEMP RAVEMEMS REPORT OCR DIAGNOSTIC

on: [push]

permissions:
  contents: write

jobs:
  append:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: RAPPORT
          fetch-depth: 0
      - name: Append OCR diagnostic
        run: python tools/temp_append_ravemems_ocr_diagnostic.py
      - name: Commit report and remove temporary tooling
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions-bot
          git config user.email github-actions@users.noreply.github.com
          git rm .github/workflows/temp-ravemems-report-ocr-diagnostic.yml .ravemems-ocr-report-trigger tools/temp_append_ravemems_ocr_diagnostic.py
          git add RAPPORT_SUIVI_ECU_MEMS_MANAGER.md
          expected="$(printf '%s\n' '.github/workflows/temp-ravemems-report-ocr-diagnostic.yml' '.ravemems-ocr-report-trigger' 'RAPPORT_SUIVI_ECU_MEMS_MANAGER.md' 'tools/temp_append_ravemems_ocr_diagnostic.py' | sort)"
          actual="$(git diff --cached --name-only | sort)"
          test "$actual" = "$expected"
          git commit -m "Record refined RAVEMEMS TEST2 OCR diagnosis"
          git push origin HEAD:RAPPORT
