name: TEMP journal RAVEMEMS additive merge audit execution

on:
  push:
    branches: ["RAPPORT"]
    paths: [".github/workflows/tmp-ravemems-journal-additive-audit-execution.yml"]

permissions:
  contents: write

jobs:
  journal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: RAPPORT
          fetch-depth: 1
      - name: Append additive audit execution plan and self-clean
        shell: bash
        run: |
          cat >> RAPPORT_SUIVI_ECU_MEMS_MANAGER.md <<'EOF'

## 2026-09-01 - RAVEMEMS - AVANT AUDIT DE FUSION ADDITIVE DANS LA BASE #103

Regle utilisateur confirmee et prioritaire : RAVEMEMS ne remplace jamais le contenu historique de la base ECU MEMS Manager. La base SQLite effectivement emballee dans BUILD #103 reste la base mere. RAVEMEMS doit etre ajoute dans une copie de cette base, puis les recouvrements/doublons doivent etre identifies et traites de maniere controlee. Aucun contenu historique ne peut etre supprime pendant l import.

Sources exactes verrouillees pour l audit :
- base mere AVANT : artefact officiel BUILD #103 `9738640151`, run `33334306835`, branche `MEMSX64`, commit `1d6316bd1746d6f2b4cfb751cab88d18e27ef730` ;
- corpus RAVEMEMS : artefact `9791187684`, run `33484362718`, extracteur `72dcaf04e84181669aa25c9103ea60bf47d9e1a7`, source RAVE `643de091b474f4e27917a065bdf46d5a0c764276`, SQLite RAVEMEMS SHA-256 `8f2cb17525efb2c1b296bc675c781740a9db3e7de84f2e4150f3a3ebbe49a87b`.

Plan d audit autorise sur copie uniquement :
1. telecharger les deux artefacts officiels sans re-extraire RAVE ;
2. localiser le SQLite reel du BUILD #103 et enregistrer son schema complet, compteurs par table, integrite, FK et empreintes logiques des tables historiques ;
3. copier cette base et lui appliquer le socle documentaire multilingue V1 deja valide, sans modifier les tables historiques ;
4. importer additivement RAVEMEMS dans les tables `mems_doc_*` avec des cles espacees/namespaced RAVEMEMS, en conservant 47 documents, 1359 pages, textes source, assets, OCR et provenance ;
5. verifier apres import que toutes les tables historiques ont exactement les memes lignes/empreintes logiques qu avant ;
6. produire un inventaire des candidats doublons/recouvrements entre contenu historique et RAVEMEMS, sans supprimer ni fusionner automatiquement ;
7. classer les candidats en identique / complementaire / conflit / a revoir avant toute operation de dedoublonnage ;
8. produire un rapport AVANT / APRES AJOUT / CANDIDATS DOUBLONS et un SQLite fusionne uniquement comme artefact de test.

Interdictions : aucune suppression automatique de doublon, aucun remplacement de la base, aucun QZ64/installateur final, aucune modification `MEMSX64`, protocole, ECU, UI, IA ou ONNX pendant cet audit.

PROCHAINE ACTION EXACTE : pousser uniquement le workflow/script temporaire d audit de fusion additive sur une branche RAVEMEMS hors production, executer l audit contre les deux artefacts exacts, puis journaliser les resultats avant toute decision de dedoublonnage.
EOF
          rm .github/workflows/tmp-ravemems-journal-additive-audit-execution.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add RAPPORT_SUIVI_ECU_MEMS_MANAGER.md .github/workflows/tmp-ravemems-journal-additive-audit-execution.yml
          git commit -m "Record RAVEMEMS additive merge audit execution plan"
          git push origin HEAD:RAPPORT
