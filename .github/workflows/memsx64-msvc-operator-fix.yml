name: Apply isolated MEMSX64 MSVC operator fix

on:
  push:
    branches: [MEMSX64]
    paths:
      - '.github/workflows/memsx64-msvc-operator-fix.yml'

permissions:
  contents: write

jobs:
  apply-fix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout MEMSX64
        uses: actions/checkout@v4
        with:
          ref: MEMSX64

      - name: Replace only the recorded failing operator
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          path = Path('mainwindow.cpp')
          text = path.read_text(encoding='utf-8')
          old = 'if (((data->idle_switch)!= 0) or ((data->uk3)!=16))'
          new = 'if (((data->idle_switch) != 0) || ((data->uk3) != 16))'
          count = text.count(old)
          if count != 1:
              raise SystemExit(f'Expected exactly one recorded expression, found {count}')
          path.write_text(text.replace(old, new, 1), encoding='utf-8', newline='')
          PY
          git diff --check
          test "$(git diff --numstat -- mainwindow.cpp)" = $'1\t1\tmainwindow.cpp'

      - name: Commit only the one-line program fix and remove this helper
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/memsx64-msvc-operator-fix.yml
          git add -- mainwindow.cpp
          git diff --cached --check
          git commit -m "Fix MSVC logical operator in mainwindow"
          git push origin HEAD:MEMSX64
