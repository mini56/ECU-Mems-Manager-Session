name: TEMP journal real RAVEMEMS package test start
on:
  push:
    branches: [RAPPORT]
    paths: ['.github/workflows/tmp-journal-realtest-ravemems-start.yml']
permissions:
  contents: write
jobs:
  journal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: RAPPORT
          fetch-depth: 0
      - name: Append real test checkpoint and self-delete
        shell: bash
        run: |
          set -euo pipefail
          cat >> RAPPORT_SUIVI_ECU_MEMS_MANAGER.md <<'EOF'

## 2026-09-01 — TEST REEL PACKAGE RAVEMEMS VISUEL x64 : DEMARRAGE UTILISATEUR OK

Package testé : `ECU-MEMS-Manager-x64-RAVEMEMS-VISUAL-TEST-from-BUILD-103`, artefact `9811065597`, run `33533678990` SUCCESS.

Premier retour utilisateur réel :
- téléchargement : OK ;
- extraction du ZIP : OK ;
- démarrage de l'application : OK ;
- premières captures de l'environnement fournies ;
- fonction `Vue instantanée` / capture : OK, plusieurs captures sont bien enregistrées dans `captures/...png` ;
- navigation visible sur les premiers onglets sans anomalie bloquante signalée à ce stade ;
- prochain test utilisateur : onglet `IA MEMS`, réponses IA et fonctionnement réel du bouton `Voir le schéma`.

Aucune modification technique autorisée sur la base de ce premier checkpoint. Attendre les résultats du test IA réel et journaliser immédiatement tout défaut ou validation.
`MEMSX64` doit rester strictement sur BUILD #103 `1d6316bd1746d6f2b4cfb751cab88d18e27ef730`.
EOF
          rm .github/workflows/tmp-journal-realtest-ravemems-start.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add RAPPORT_SUIVI_ECU_MEMS_MANAGER.md .github/workflows/tmp-journal-realtest-ravemems-start.yml
          git commit -m "Record start of real RAVEMEMS x64 user test"
          git push HEAD:RAPPORT
